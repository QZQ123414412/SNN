# 负脉冲激发统计功能说明

## 功能概述

该功能用于统计和分析 SignedIF 神经元在推理过程中的正负脉冲激发情况。

## 实现细节

### 1. SignedIF 类的修改（`models/layer.py`）

#### 新增的统计变量
```python
self.pos_spike_count = 0  # 正脉冲激发次数
self.neg_spike_count = 0  # 负脉冲激发次数
self.total_neurons = 0     # 总神经元数
```

#### 新增的方法

**`reset_stats()`**: 重置统计信息
```python
def reset_stats(self):
    """重置统计信息"""
    self.pos_spike_count = 0
    self.neg_spike_count = 0
    self.total_neurons = 0
```

**`get_stats()`**: 获取统计信息
```python
def get_stats(self):
    """获取统计信息"""
    return {
        'pos_spike_count': self.pos_spike_count,
        'neg_spike_count': self.neg_spike_count,
        'total_neurons': self.total_neurons,
        'pos_spike_rate': self.pos_spike_count / max(self.total_neurons, 1),
        'neg_spike_rate': self.neg_spike_count / max(self.total_neurons, 1)
    }
```

#### forward 方法中的统计代码
```python
# 统计脉冲激发情况（只在推理时统计）
if self.training == False:
    pos_fired = (pos_spike != 0).sum().item()
    neg_fired = (neg_spike != 0).sum().item()
    self.pos_spike_count += pos_fired
    self.neg_spike_count += neg_fired
    self.total_neurons += x[t].numel()
```

### 2. 测试脚本的修改（`main_test_signed.py`）

#### 测试前：重置统计
```python
# 重置所有 SignedIF 层的统计信息
for module in model.modules():
    if isinstance(module, SignedIF):
        module.reset_stats()
```

#### 测试后：打印统计
```python
# 收集所有层的统计信息
for name, module in model.named_modules():
    if isinstance(module, SignedIF):
        stats = module.get_stats()
        # 打印统计信息
```

## 输出示例

运行测试后，会看到类似以下的输出：

```
====================================================================================================
负脉冲激发统计:
====================================================================================================

层名称                                       正脉冲          负脉冲          正比例       负比例       负脉冲激发?
----------------------------------------------------------------------------------------------------
layer1.2                                     145,234         12,456            2.3456%     0.2012%  ✓ 是
layer1.6                                     234,567         0                 3.7890%     0.0000%  ✗ 否
layer2.2                                     345,678         23,456            5.5678%     0.3789%  ✓ 是
layer2.6                                     456,789         34,567            7.3456%     0.5567%  ✓ 是
layer3.2                                     567,890         0                 9.1234%     0.0000%  ✗ 否
layer3.6                                     678,901         45,678           10.9012%     0.7345%  ✓ 是
layer3.10                                    789,012         56,789           12.6789%     0.9123%  ✓ 是
layer4.2                                     890,123         0                14.4567%     0.0000%  ✗ 否
layer4.6                                     901,234         67,890           14.5234%     1.0901%  ✓ 是
layer4.10                                    123,456         0                 1.9890%     0.0000%  ✗ 否
layer5.2                                     234,567         78,901            3.7723%     1.2679%  ✓ 是
layer5.6                                     345,678         89,012            5.5567%     1.4301%  ✓ 是
layer5.10                                    456,789         90,123            7.3401%     1.4490%  ✓ 是
classifier.2                                 567,890         12,345            9.1245%     0.1984%  ✓ 是
classifier.5                                 678,901         23,456           10.9089%     0.3768%  ✓ 是
----------------------------------------------------------------------------------------------------
总计                                        6,416,709       534,673            7.2345%     0.6023%

激发负脉冲的层数: 11/15
负脉冲占总脉冲比例: 7.69%
====================================================================================================
```

## 统计指标说明

### 1. 正脉冲 (pos_spike_count)
- 该层在所有时间步中激发的正脉冲总数
- 计算方式：统计所有 `pos_spike != 0` 的神经元数量

### 2. 负脉冲 (neg_spike_count)
- 该层在所有时间步中激发的负脉冲总数
- 计算方式：统计所有 `neg_spike != 0` 的神经元数量

### 3. 正比例 (pos_spike_rate)
- 正脉冲激发率 = 正脉冲数 / 总神经元数
- 表示平均每个神经元激发正脉冲的次数

### 4. 负比例 (neg_spike_rate)
- 负脉冲激发率 = 负脉冲数 / 总神经元数
- 表示平均每个神经元激发负脉冲的次数

### 5. 负脉冲激发?
- ✓ 是：该层有负脉冲激发
- ✗ 否：该层没有负脉冲激发

### 6. 负脉冲占总脉冲比例
- 负脉冲数 / (正脉冲数 + 负脉冲数)
- 反映负脉冲在整体脉冲中的占比

## 使用方法

```bash
cd d:\ANN2SNN\QCFS
python main_test_signed.py -data=cifar100 -arch=vgg16_signed -id=cifar100-vgg16-l8-example -T=16 -dev=0
```

## 分析要点

### 1. 哪些层激发了负脉冲？
查看 "负脉冲激发?" 列，标记为 "✓ 是" 的层激发了负脉冲。

### 2. 负脉冲激发的频率如何？
查看 "负比例" 列：
- 高比例（>1%）：该层频繁使用负脉冲进行校正
- 低比例（<0.5%）：该层较少使用负脉冲
- 0%：该层未使用负脉冲

### 3. 负脉冲的分布规律
- 前几层（layer1、layer2）：通常负脉冲较少
- 中间层（layer3、layer4）：可能有更多负脉冲
- 后面层（layer5、classifier）：取决于任务复杂度

### 4. 负脉冲的作用
- 负脉冲用于校正过度激活
- 帮助实现双向编码
- 提高表达能力

## 技术细节

### 为什么只在推理时统计？
```python
if self.training == False:  # 只在推理时统计
```
- 训练时统计会影响性能
- 推理时的统计更有意义（评估模型行为）

### 统计粒度
- 每个时间步单独统计
- 累加所有 batch 的结果
- 最终得到整个测试集的统计

### 内存效率
- 只统计计数，不保存具体的脉冲值
- 内存开销很小

## 常见问题

### Q1: 为什么某些层没有负脉冲？
**A:** 可能原因：
1. 该层的激活值总是正的，不需要负脉冲校正
2. 膜电位没有低于负阈值
3. 该层的神经元还没有传输过正脉冲（负脉冲的前提条件）

### Q2: 负脉冲比例多少算正常？
**A:** 取决于任务和网络结构：
- 典型范围：0.5% - 5%
- 如果某层负脉冲 > 10%，可能需要检查该层的阈值设置

### Q3: 如何提高负脉冲的利用率？
**A:** 
1. 调整阈值参数
2. 增加时间步数
3. 调整膜电位初始化值

## 扩展功能

可以进一步添加：
1. 每个时间步的详细统计
2. 膜电位分布统计
3. 可视化脉冲激发热图
4. 不同样本的脉冲模式对比
