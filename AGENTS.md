# QCFS 项目协作规范

## 1. 适用范围

本文件适用于整个 QCFS 仓库，包括模型实现、校准方法、实验脚本、测试、文档、实验结果和图表。

规则优先级：用户明确指令 > 本文件 > README 和其他项目文档。

`必须`、`禁止`属于强制规则；`建议`属于默认工程指导，可根据任务合理调整。

## 2. 文件与操作安全

- 禁止批量删除文件或目录。
- 禁止使用 `del /s`、`rd /s`、`rmdir /s`、`Remove-Item -Recurse`、`rm -rf`。
- 删除文件时，一次只能删除一个经过确认的明确路径，禁止使用通配符。
- 如果需要删除多个文件或目录，停止操作并请用户手动处理。
- 禁止使用 `git reset --hard`、强制切换或其他可能覆盖工作区的操作。
- 不得恢复、覆盖或删除不是当前任务产生的工作区改动。
- 不得删除或修改数据集、checkpoint，除非用户明确要求。
- 不提交 `__pycache__`、日志、临时文件和模型权重。
- 运行 Python 时优先使用 `-B`，避免生成或修改字节码文件。

## 3. 项目目录约定

- 训练入口放在 `scripts/train/`。
- 模型评估入口放在 `scripts/evaluate/`。
- 消融与统计实验放在 `scripts/experiments/`。
- 公共校准逻辑放在 `calibration.py` 或已有公共模块中。
- 单元测试放在 `tests/`。
- 方法和指标说明放在 `docs/methodology/`。
- 正式实验结果放在 `docs/results/`。
- 开发验证、失败尝试和外部分支日志放在 `docs/archive/`。
- 论文图表放在 `figures/`。

## 4. 实验结果强制规则

- 运行实验前必须检查已有报告，避免无意义重复运行。
- 必须确认数据集、模型、checkpoint、配置、时间步、随机种子和校准参数。
- 禁止静默覆盖已有结果；复跑时使用新文件名，或先获得用户确认。
- 正式结果、参数筛选、诊断结果和失败尝试必须分开存放。
- 新结果必须登记到 `docs/results/MANIFEST.md`。
- MANIFEST 必须记录实验内容、生产脚本、数据集、配置、来源等级和结果状态。
- 人工汇总不能标记为脚本直接产物。
- 日志不能作为正式实验报告。
- 图表必须能够追溯到数据报告；新增图表时应同时保存绘图脚本。
- 移动结果后必须更新脚本默认输出路径、README、实验协议和相关文档引用。

结果目录使用以下分类：

- `docs/results/baseline/`：准确率和脉冲统计基线。
- `docs/results/comparative_ablation/`：严格控制变量的跨方法消融。
- `docs/results/state_low_rank_ftbc/`：state-low-rank FTBC 正式结果和回归验证。
- `docs/results/csrr/final/`：CSRR 正式结果。
- `docs/results/csrr/screening/`：参数筛选。
- `docs/results/csrr/diagnostics/`：冒烟和机制诊断。
- `docs/results/csrr/analysis/`：人工综合分析。
- `docs/archive/experiments/`：实现验证和失败尝试。

## 5. 实验执行流程

- 优先使用已有数据集和 checkpoint，不重复下载或训练。
- 首次运行新配置时先执行小时间步或少批次冒烟测试。
- 冒烟测试通过后再运行完整的 `T=1,2,4,8,16,32` 实验。
- 对比实验必须保持数据、模型、checkpoint、校准批次和随机种子一致。
- 必须明确每组配置是否启用 QCFS、SNM、R0、完整 FTBC、low-rank FTBC 和 CSRR。
- 实验中断时保留日志，不得将不完整报告标记为正式结果。
- 完成后检查总表、逐层指标和配置元数据是否齐全。

## 6. 代码开发指导

- 建议先阅读相关实现、测试和历史结果，再修改代码。
- 建议沿用已有配置结构和辅助函数，避免重复实现实验逻辑。
- 建议保持改动范围集中，不进行与任务无关的重构。
- 修改共享模型、校准或统计逻辑时，应增加或更新测试。
- 只修改文档或移动结果时，不强制新增测试，但必须验证路径。
- 实验脚本应自动定位仓库根目录并创建输出父目录。
- 对随机性、设备、数据批次和计数口径进行显式控制。
- 注释只解释非显然逻辑，不重复描述代码表面行为。

## 7. 验证要求

完成代码修改后至少执行：

```powershell
D:\Anaconda\envs\ann2snn\python.exe -B -m unittest discover tests
```

同时检查：

- 修改过的 Python 文件能够编译。
- 实验入口的 `--help` 可以正常运行。
- `git diff --check` 无格式错误。
- 文档中不存在失效的旧结果路径。
- Git 状态中不存在当前任务意外生成的文件。
- 原有用户改动没有被覆盖或恢复。

无法执行某项验证时，必须在最终说明中明确指出。

## 8. Git 与交付

- 未经用户明确要求，不提交、不推送、不创建 PR。
- 不修改与当前任务无关的提交历史。
- 提交应按测试、实现、结果或文档划分清晰范围。
- 最终说明必须列出主要改动、验证结果、未完成事项和保留的原有改动。
